{
  stylix = {
    targets = {
      nixvim.enable = false;
      # kitty = {
      #   # Kitty's colors are broken
      #   enable = false;
      # };
      qt = {
        platform = "qtct";
      };
    };
  };
}
